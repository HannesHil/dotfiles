# Config for sway
# Read `man 5 sway` for a complete reference.
#
# All sway.d/99_*.conf files are in .gitignore

set $CONFIGPATH /home/hannes/.config/sway

include $CONFIGPATH/sway.d/*.conf
#include $CONFIGPATH/$(hostname)/*.conf
